name: Water Pump
version: "1.0"
slug: waterpump
description: A water pump device.
startup: application
boot: auto
host_network: true
arch:
  - amd64
  - aarch64
services:
  - mqtt:need
options:
  broker_url: core-mosquitto
  broker_port: 1883
  device_id: "1"
  device_name: "Water Pump"
schema:
  broker_url: str
  broker_port: int
  device_id: str
  device_name: str
